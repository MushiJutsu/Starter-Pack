return {
    ---this is the value that decides if extrasounds is on or not
    ---you dont need to change it internally here, this value only decides the default value if you never played the mod before
    ---the player's preferences on config are stored in their profile
    ---check Volatile Humor's calculation code for how to check this value
    extrasounds = true,

    ---Config value for if chips/mult/dollar values on jokers will be rounded or not
    stpck_rounding = true,
}
